fun main(){
	// Definning a variable
	var MyAge: Int = 24
	
	if (MyAge < 18){
		println("You are too young")}
	else if (MyAge < 60){
		println("You are old enough")}
	else{
		println("You are too old")}
}
