// Saving an Array
const name_list = ['David Bowman', 'Frank Poole', 'Heywood Floyd', 'HAL 9000'];

// Printing the names and the length of the list
name_list.forEach(name_list => console.log(name_list));
